---
- name: Getting Started With Automation
  hosts: all
  gather_facts: false

  tasks:
    - name: SHOW COMMANDS
      ios_command:
        commands:
         - show version | sec IOS
         - show ip route
      register: output
    - debug: var=output.stdout_lines

