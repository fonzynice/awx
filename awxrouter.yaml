---
- name: Getting Started With Automation
  hosts: 172.16.118.134
  gather_facts: false

  tasks:
    - name: SHOW COMMANDS
      ios_command:
        commands:
         - show ip int brief
         - show ip route
      register: output
    - debug: var=output.stdout_lines

